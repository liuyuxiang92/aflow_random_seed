# WS20230512
#
# UNIT TEST
#
# Run unit_test when a pull request is edited
#

name: AFLOW inhouse build Unit Test Checks
#c++ builds: https://github.community/t/conditional-matrices/17206

run-name: Unit Test

on: # Run unit_test whenever a pull request is opened or committed to
  pull_request:
    types: [opened, reopened, synchronize, edited]
jobs:
  Unit_Test:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - uses: actions/checkout@v3
      - name: make
        run: make -C src
      - name: make unit_test
        run: |
          cd src
          make unit_test
